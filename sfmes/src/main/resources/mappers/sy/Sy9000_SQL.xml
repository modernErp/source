<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="sfmes.sqlmap.sy">
    <!-- ModernViewer 조회용 SQL 실행 -->
    <select id="selectSy9000_MDNV" parameterType="LinkedHashMap" resultType="LinkedHashMap">
    <![CDATA[
        ${SQL_STR}
    ]]>
    </select>

    <!-- ModernViewer CUD용 SQL 실행 -->
    <update id="updateSy9000_MDNV" parameterType="LinkedHashMap">
    <![CDATA[
        ${SQL_STR}
    ]]>
    </update>
</mapper>